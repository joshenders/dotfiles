# Select the instruction set to use when disassembling the program via the
# disassemble or x/i commands. Currently this command is only defined for the
# Intel x86 family. You can set instruction-set to either intel or att. The
# default is att, the AT&T flavor used by default by Unix assemblers for
# x86-based targets.
set disassembly-flavor intel
